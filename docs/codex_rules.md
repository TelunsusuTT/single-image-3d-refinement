# Codex Rules

Work only inside:

```bash
/vol/bitbucket/ct1022/hy3dpaint_finetune
```

Do not modify:

```bash
/vol/bitbucket/ct1022/Hunyuan3D2.1_Work
```

Do not inspect or modify:

```bash
/vol/bitbucket/ct1022/seamtex
```

Do not install packages, run training, run Hunyuan, run Blender, clone datasets, create large files, or commit automatically.

For Phase 0, only create the scaffold, project-local config copy script, local sanity script, storage report script, and Slurm smoke-test launcher.
